# Idle configuration
exec swayidle -w \
    timeout 2400 'swaylock --config ~/.config/swaylock/config' \
    timeout 3600 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
    before-sleep 'swaylock -f --config ~/.config/swaylock/config'

